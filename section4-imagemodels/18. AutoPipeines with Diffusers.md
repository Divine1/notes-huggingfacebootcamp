
```
https://huggingface.co/dreamlike-art/dreamlike-photoreal-2.0

dreamlike-art/dreamlike-photoreal-2.0


https://huggingface.co/docs/diffusers/main/en/api/pipelines/auto_pipeline#diffusers.AutoPipelineForImage2Image.from_pretrained.example-2
```


```
from diffusers import AutoPipelineForText2Image
import torch
pipeline = AutoPipelineForText2Image.from_pretrained("dreamlike-art/dreamlike-photoreal-2.0",
                                                     torch_dtype=torch.float16, use_safetensors=True).to("cuda")
prompt = "cinematic photo of big bird in paris, 35mm photo, film"
image = pipeline(prompt).images[0]
image

```


```
https://huggingface.co/docs/diffusers/api/pipelines/stable_diffusion/img2img```


```





```

sudo apt install python3 python3-pip python3-venv -y
python3 -m venv jupyter_env
source jupyter_env/bin/activate
pip install jupyter
jupyter notebook --generate-config
jupyter --config-dir
vi /root/.jupyter/jupyter_notebook_config.py
python -m jupyter_server.auth password

jupyter notebook --allow-root


ufw allow 8888/tcp
ufw enable



c = get_config()
c.NotebookApp.ip = '0.0.0.0'                  # Listen on all network interfaces
c.NotebookApp.port = 8888                     # Choose desired port
c.NotebookApp.open_browser = False            # Avoid auto-opening browser on the VM
c.NotebookApp.password_required = True        # Enforce password auth
c.NotebookApp.allow_remote_access = True


python -m jupyter_server.auth password
password divine


```






```

!pip install transformers==4.41.0
!pip install diffusers[torch,torchvision,transformers,accelerate]

#from .autonotebook import tqdm as notebook_tqdm
from diffusers import StableDiffusionImg2ImgPipeline
from PIL import Image
import torch

model_id = "dreamlike-art/dreamlike-photoreal-2.0"
pipe = StableDiffusionImg2ImgPipeline.from_pretrained(model_id, torch_dtype=torch.float16)
pipe = pipe.to("cuda")

init_image = Image.open("/kaggle/input/divdata1/keer1.jpg").convert("RGB")

prompt = "Astronaut in a jungle, cold color palette, muted colors, detailed, 8k"
image = pipe(prompt=prompt, image=init_image, strength=0.6).images

image[0]

```
